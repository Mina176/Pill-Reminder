import 'package:flutter/material.dart';

class AddMedBody extends StatelessWidget {
  const AddMedBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}