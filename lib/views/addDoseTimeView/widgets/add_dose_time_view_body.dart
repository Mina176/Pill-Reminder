import 'package:flutter/material.dart';

class AddDoseTimeViewBody extends StatelessWidget {
  const AddDoseTimeViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
